name: appbangiay
description: Ứng dụng bán giày Sneaker Store 👟 với Flutter + Firebase.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # --- Firebase Packages ---
  firebase_core: ^3.4.0
  firebase_auth: ^5.3.1
  firebase_database: ^11.0.3

  # --- State Management ---
  provider: ^6.1.2

  # --- Local Storage ---
  shared_preferences: ^2.2.3

  # --- UI / Animation / Icons ---
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/
